# BiddingFlow 프론트엔드 개요

이 문서는 2026년 8월 24일 기준 BiddingFlow 프론트 앱의 목적, 현재 구현 범위, 중간 발표용 시연 시나리오와 후속 개발 항목을 정리한다. 세부 변경 이력은 [`log.txt`](./log.txt)에서 확인할 수 있다.

## 1. 앱의 목적

BiddingFlow는 ERPNext에서 발생한 구매 요청(Material Request, MR)을 구매 담당자가 한 화면에서 확인하고, 자동화 파이프라인의 진행 상태와 사람의 판단이 필요한 지점을 관리하기 위한 구매 업무 UI다.

현재 UX의 핵심 원칙은 다음과 같다.

- 정해진 구매 절차는 백엔드 자동화가 진행한다.
- 사용자의 판단이 필요한 지점에서는 챗봇 대화 대신 구조화된 폼을 제시한다.
- 자동화가 수행한 단계와 결과를 작업별 이력으로 투명하게 보여준다.
- 잘못 처리된 이전 단계는 전체 작업을 초기화하지 않고 정정 분기를 만들어 재실행한다.
- 최종 공급사 선정이나 승인처럼 책임이 필요한 결정은 사용자에게 남겨 둔다.

## 2. 현재 시연 범위

중간 발표에서는 개인별 카테고리 라우팅과 작업 이관을 제외하고, ERPNext에서 수신한 모든 MR을 한 명의 구매 담당자가 처리하는 **통합 작업함** 시나리오를 사용한다.

현재 화면 데이터와 자동화 실행은 대부분 프론트엔드 목업이다. ERPNext 로그인과 접속자 프로필 표시는 실제 백엔드 API에 연결되어 있지만, 작업 목록·상태 변화·도움말 응답·스케줄러 실행은 시연을 위한 인메모리 데이터와 상호작용으로 구성되어 있다.

## 3. 화면 구성

### 3.1 도움말 챗봇

프로그램의 기능과 사용법을 안내하는 전역 도움말 UI다. 개별 구매 작업을 수행하는 에이전트 세션과는 분리되어 있다.

- 통합 작업함, 사용자 확인 폼, 알림, Flow Scheduler 사용법 안내
- RFQ, 견적 비교, 공급사 선정, PO 발주 흐름 설명
- 새 도움말 대화 및 최근 대화 목록 제공
- 현재는 키워드 기반 목업 응답이며 실제 구매 작업은 실행하지 않음

### 3.2 Flow Scheduler

반복 실행하거나 이벤트를 감시하는 자동화 파이프라인의 상태를 확인하는 화면이다.

- MR 감시, 시장 시세 확인, 소액 구매 등의 파이프라인 예시
- 파이프라인 활성화/비활성화 및 즉시 실행 상호작용
- 사용자 승인 대기 항목과 자동화 실행 로그 표시
- 현재는 프론트엔드 시연용 상태 변경으로 동작

### 3.3 구매 작업 — 통합 작업함

현재 앱의 메인 화면이다. ERPNext에서 수신한 MR을 긴 작업 카드인 **스트립** 단위로 표시한다.

- 확인 필요, 진행 중, 외부 응답 대기, 요청자 보완 대기, 완료 상태 필터
- 작업명, 현재 단계, 진행 단계 수, 상태, 우선순위와 수신 시각 표시
- 상태색은 상단 진행선과 작은 상태점에 제한해 정보 위계를 유지
- 완료된 작업은 비활성화된 항목처럼 저채도 회색으로 표시
- 작업 카드를 누르면 현재 단계 중심의 워크플로가 아래로 펼쳐짐
- 워크플로 영역은 가로 스크롤로 전체 단계를 탐색할 수 있음
- 작업을 열면 요청자, 카테고리, 마감일, 처리 담당자와 자동화 이력을 확인할 수 있음

## 4. 사용자 개입 방식

자동화가 모호한 데이터나 중요한 판단을 만나면 작업 상태를 `확인 필요`로 바꾸고 구조화된 폼을 표시한다.

현재 목업은 ERPNext 구매 요청의 비정형 값을 정리하는 상황을 예시로 사용한다.

1. ERPNext 요청 원문과 제안된 표준값을 나란히 보여준다.
2. 사용자가 품목명, 규격, 수량 등의 표준값을 확인하거나 수정한다.
3. `적용 후 자동화 재개` 또는 `요청자에게 보완 요청`을 선택한다.
4. 원문, 사용자 결정과 자동화 결과는 실행 이력에 보존한다.

이전 단계의 스냅숏에서 오류가 발견되면 전체 작업을 하드 리셋하지 않는다. 해당 단계에 정정 분기를 추가하고, 기존 문맥과 감사 이력을 유지한 채 영향을 받는 후속 단계만 다시 실행하는 방향으로 설계했다.

## 5. 알림 UX

견적 도착, 사용자 확인 필요, 승인 완료, 발주 완료와 같은 상태 변화는 다음 두 방식으로 전달할 수 있도록 UI를 준비했다.

- 상단 알림 버튼과 드롭다운에서 누적 이벤트 확인
- 우측 하단 토스트로 실시간 상태 변화 안내

알림을 선택하면 관련 작업 또는 필요한 사용자 개입 위치로 이동하는 흐름을 목표로 한다.

## 6. ERPNext 로그인 및 사용자 표시

로그인 화면은 ERPNext 계정 ID 또는 이메일과 비밀번호를 백엔드의 `/api/login`으로 전달한다. 로그인 성공 후 백엔드는 ERPNext User 문서에서 사용자 정보를 조회하며, 프론트엔드는 다음 정보를 토큰 및 브라우저 저장소에 유지한다.

- ERPNext 사용자 ID
- 전체 이름(`full_name`)
- 이메일과 사용자명
- 사용자 유형

사이드바 계정 영역과 작업 상세의 처리 담당자에는 실제 접속자 이름을 표시한다. `Administrator`처럼 이메일 형식이 아닌 ERPNext 계정도 지원한다. 브라우저 저장 키는 `erp_user_profile`이며 로그아웃 시 토큰과 함께 제거된다.

> `.env`, API Key, API Secret, ERPNext 세션 쿠키는 프론트 문서나 저장소에 기록하지 않는다.

## 7. 주요 파일

```text
src/
├─ App.jsx                                  # 인증과 3개 워크스페이스 상태 관리
├─ index.css                                # 공통 레이아웃과 전역 스타일
├─ utils/auth.js                            # 토큰·ERP 사용자 프로필 보존 및 갱신
└─ components/
   ├─ auth/LoginPage.jsx                    # ERPNext 로그인 화면
   ├─ sidebar/Sidebar.jsx                   # 내비게이션·세션·사용자 계정 영역
   ├─ chat/ChatWorkspace.jsx                # 전역 도움말 챗봇 UI
   ├─ scheduler/SchedulerDashboard.jsx      # 자동화 스케줄러 목업
   └─ operations/
      ├─ OperationsWorkspace.jsx            # 통합 작업함·인터럽트·알림 로직
      └─ OperationsWorkspace.css            # 작업 스트립과 상세 화면 스타일
```

## 8. 로컬 실행

### 백엔드

형제 저장소 `SKN31-FINAL-3Team`에서 가상환경과 `.env`를 준비한 뒤 FastAPI 서버를 실행한다.

```powershell
cd ..\SKN31-FINAL-3Team
.\.venv\Scripts\python.exe main.py
```

백엔드는 기본적으로 `http://127.0.0.1:8000`에서 실행된다.

### 프론트엔드

```powershell
cd ..\SKN31-FINAL-front
npm install
npm run dev
```

Vite 개발 서버는 `/api` 요청을 로컬 백엔드의 `http://127.0.0.1:8000`으로 프록시한다.

## 9. 검증 명령

```powershell
npm run lint
npm run build
```

현재 개편본은 위 두 검사를 통과했으며 1366×768, 1600×1000 브라우저 크기에서 주요 화면 렌더링을 확인했다.

## 10. 후속 개발 항목

- ERPNext MR 웹훅 수신 결과를 실제 작업 목록과 연결
- 작업 상태, 체크포인트와 사용자 입력을 서버에 영속화
- 백엔드 LangGraph 인터럽트 스키마를 프론트 폼 컴포넌트로 매핑
- 공급사 후보, 견적 비교 등 단계별 결과를 테이블/비교 카드로 표현
- WebSocket 또는 Server-Sent Events 기반 실시간 작업 상태 및 토스트 알림
- 알림에서 작업 상세와 인터럽트 폼으로 직접 이동
- 발표 이후 규칙 기반 라우팅과 AI 보조 분류, 담당자별 작업함, 오배정 이관 기능 재도입 검토
- 접근성, 반응형 레이아웃, 로딩·오류·빈 상태 보강

## 11. 이번 작업 요약

- 개인별 업무 분류 화면을 중간 발표용 단일 담당자 통합 작업함으로 변경
- 작업 스트립, 펼침 워크플로, 완료 상태와 정보 위계 전면 리파인
- 작업별 챗봇을 제거하고 폼 기반 사용자 인터럽트 UX로 전환
- 이전 단계 스냅숏과 정정 분기 개념을 UI에 반영
- 전역 프로그램 도움말 챗봇 UI 복구
- 알림 드롭다운과 우측 하단 토스트 목업 준비
- ERPNext 실제 로그인 사용자 이름과 계정 ID 표시
- 프론트 빌드·린트 및 백엔드 프로필 토큰 갱신 흐름 검증

