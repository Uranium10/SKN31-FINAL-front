# Vite development proxy target. Browser requests should remain relative /api/*.
VITE_API_BASE_URL=http://127.0.0.1:8000

# mock: 기존 시연 데이터만 사용
# hybrid: API 우선, 연결 실패 시 시연 데이터 유지
# api: 실제 ERPNext/PostgreSQL 작업만 표시
VITE_PROCUREMENT_DATA_MODE=mock
